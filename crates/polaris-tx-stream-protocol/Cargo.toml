[package]
name = "polaris-tx-stream-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Bounded borrowed decoder for Polaris Lab canonical resolved transactions"
publish = false

[dependencies]
thiserror.workspace = true

[dev-dependencies]
proptest = "1"
