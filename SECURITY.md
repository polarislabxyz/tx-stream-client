# Security reports

Use GitHub private vulnerability reporting for this repository once enabled.
Do not include API keys or customer traffic in public issues. If private
reporting is unavailable, contact your existing Polaris Lab support representative.

Use immutable release tags and verify the contract manifest. The decoder checks
frame structure and transaction sanitization, not cryptographic authenticity or
Solana execution/finality. TLS authenticates the remote server.
